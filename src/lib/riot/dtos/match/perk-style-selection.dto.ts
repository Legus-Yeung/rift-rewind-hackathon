/** Perk Style Selection DTO */
export interface PerkStyleSelectionDto {
  /** (int) Perk id (rune id). */
  perk?: number;

  /** (int) var1 (additional stat). */
  var1?: number;

  /** (int) var2 (additional stat). */
  var2?: number;

  /** (int) var3 (additional stat). */
  var3?: number;
}

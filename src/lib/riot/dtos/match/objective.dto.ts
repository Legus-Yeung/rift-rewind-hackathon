/** Objective DTO */
export interface ObjectiveDto {
  /** (boolean) Whether this objective was taken first by the team. */
  first?: boolean;

  /** (int) Number of kills/takes for this objective. */
  kills?: number;
}

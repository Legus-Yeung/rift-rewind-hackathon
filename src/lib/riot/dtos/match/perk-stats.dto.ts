/** PerkStatsDto (statPerks) */
export interface PerkStatsDto {
  /** (int) Defense stat perk. */
  defense?: number;

  /** (int) Flex stat perk. */
  flex?: number;

  /** (int) Offense stat perk. */
  offense?: number;
}
